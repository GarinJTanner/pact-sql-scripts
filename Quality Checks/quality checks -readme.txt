This is a list of server-wide queries used for quality checks and fraud detection.

The most useful fraud detection query as of late has been the veggie pact - flagged messages query.

Not every query is 100% correct. I have had some trouble with using JOINs as I have had no formal training.

